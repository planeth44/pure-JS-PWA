/* jshint esversion: 8 */
const SYNC_STATUS = {
  PENDING: 'pending',
  UPDATE: 'update',
  DONE: 'done',
  FAILED: 'failed'
}

const API_URL = {
  MODELS: '/api/models',
  FILE: '/api/file',
  CHECK: '/check'
}

const ROUTES = {
  FAILED: '/failed'
}
